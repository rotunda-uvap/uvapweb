import React from "react"

const PageHeader = ({text}) => {

    return (
        <h1 className="text-center py-3 md:py-6 text-2xl md:text-4xl uppercase text-ceci-gray-dark font-thin tracking-wider leading-normal font-sans">{text}</h1>
      )
  }

  export default PageHeader


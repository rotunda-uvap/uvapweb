import React from "react"

const PageHeader = ({text}) => {
   
    return (
<h4 className="text-center py-6 uppercase text-gray-700 font-thin tracking-widest leading-normal font-sans">{text}</h4>

      )
  }
  
  export default PageHeader


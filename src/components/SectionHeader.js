import React from "react"

const SectionHeader = ({text}) => {

    return (
        <h2 className="py-4 text-center uppercase text-ceci-gray-mid text-lg md:text-xl font-thin tracking-widest mx-auto font-sans">{text}</h2>

      )
  }

  export default SectionHeader
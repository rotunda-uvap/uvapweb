import React from "react"

const SectionHeader = ({text}) => {
   
    return (
        <p className="py-2 text-center uppercase text-gray-500 text-lg font-thin tracking-widest mx-auto font-sans ">{text}</p>

      )
  }
  
  export default SectionHeader
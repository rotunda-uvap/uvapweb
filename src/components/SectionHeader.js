import React from "react"

const SectionHeader = ({text}) => {
   
    return (
        <p className="py-4 text-center uppercase text-ceci-gray-mid text-lg md:text-xl font-thin tracking-widest mx-auto font-sans ">{text}</p>

      )
  }
  
  export default SectionHeader